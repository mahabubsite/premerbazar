# Music Directory

Add your `.mp3` or `.wav` files to this directory. Later, you can reference them in the application for playback.

For example, if you place a file named `song.mp3` here, you can load it in the app via `/music/song.mp3` or by referencing the path in your code.
